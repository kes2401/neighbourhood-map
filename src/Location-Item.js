import React from 'react';

const locationItem = (props) => {

	return (

		<li className="list-item">
			<p className="list-item-text">{props.locationName}</p>">
		</li>
	)
}

export default locationItem;